<%= form_for(@div) do |f| %>
  <% if @div.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@div.errors.count, "error") %> prohibited this div from being saved:</h2>

      <ul>
      <% @div.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
